import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.franklin.phaserincremental',
  appName: 'Phaser Incremental',
  webDir: 'dist'
};

export default config;
